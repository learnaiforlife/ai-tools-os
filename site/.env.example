# Installer links come only from src/release.json generated after verification.
# No VITE_AIOS_* environment variables are used by the current site.
# Configure AIOS_DOWNLOAD_BASE_URL for scripts/verify-release.mjs after the
# signed artifacts have a final HTTPS hosting location.
